{
  "name": "sepia",
  "author": "Avik Das <adas@linkedin.com>",
  "version": "0.0.1",
  "description": "A VCR-like module that records HTTP interactions and plays them back for speed and reliability",
  "devDependencies": {
    "request": "*",
    "lodash": "*",
    "step": "*",
    "jshint": "*",
    "mocha": "1.x",
    "should": "2.x",
    "sinon": "1.x"
  },
  "scripts": {
    "test": "mocha test && sh examples/run-all-examples.sh"
  }
}
